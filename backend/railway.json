{
  "$schema": "https://schema.railway.com/railway-config.json",
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
  }
}
